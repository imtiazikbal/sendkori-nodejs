version: '3.8'

services:
  sendkori-nodejs:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - '3001:3001'
    volumes:
      - ./:/usr/src/app
    env_file:
      - .env
    command: yarn start:dev
    environment:
      PORT: 3001
    restart: always